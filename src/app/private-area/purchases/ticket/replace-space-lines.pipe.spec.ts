import { ReplaceSpaceLinesPipe } from './replace-space-lines.pipe';

describe('ReplaceSpaceLinesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReplaceSpaceLinesPipe();
    expect(pipe).toBeTruthy();
  });
});
